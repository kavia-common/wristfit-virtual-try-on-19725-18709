# Frontend environment variables for WristFit
# Set the API base URL if the backend is hosted on a different origin.
# When not set, the frontend will call the backend on the same origin (relative path).
REACT_APP_API_BASE=
